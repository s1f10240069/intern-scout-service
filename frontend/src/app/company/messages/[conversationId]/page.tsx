"use client";
import { useParams,useRouter } from "next/navigation"; import { AppShell,BackLink,PageHeading } from "@/components/AppShell"; import { Card } from "@/components/ui"; import MessageForm from "@/components/MessageForm"; import MessageThread from "@/components/MessageThread"; import { useAuthenticatedResource } from "@/hooks/useAuthenticatedResource"; import { apiFetch,ApiError } from "@/lib/api"; import { clearToken,getToken } from "@/lib/auth"; import type { Conversation } from "@/lib/types";
export default function CompanyMessageThreadPage(){const {conversationId}=useParams<{conversationId:string}>();const router=useRouter();const {data:conversation,setData,loading,error}=useAuthenticatedResource<Conversation>("company",`/company/messages/${conversationId}`,"/login?account_type=company");const send=async(body:string)=>{const token=getToken("company");if(!token)throw new Error("ログインし直してください。");try{setData(await apiFetch<Conversation>(`/company/messages/${conversationId}/messages`,{method:"POST",token,body:{message:{body}}}))}catch(err){if(err instanceof ApiError&&(err.status===401||err.status===403)){clearToken("company");router.replace("/login?account_type=company");throw new Error("ログインし直してください。")}throw new Error("メッセージを送信できませんでした。")}};return <AppShell role="company">{loading?<div className="state-card"><span className="spinner" />読み込み中...</div>:error?<div className="state-card state-card--error">{error}</div>:conversation&&<><BackLink href="/company/messages">会話一覧へ戻る</BackLink><PageHeading eyebrow="Conversation" title={`${conversation.student.name}さんとの会話`} /><Card><MessageThread conversation={conversation} viewer="company" /><MessageForm onSend={send} /></Card></>}</AppShell>}
